# 2026-03-12 工作记录

## Bridge Agent 前端组件设计文档微调

更新了 `docs/design/group-chat-bridge/frontend-components.md`，四处调整：

1. **第 3 节简化**：完成检测策略只保留空闲时间检测，移除提示符模式匹配和显式结束标记策略
2. **第 4.5 节更新**：从"需要确认"改为"已确认"——Agent 消息可以触发 @mention，触发源标识已区分 user vs agent
3. **第 5.7 节增强**：触发失败兜底机制增加 20 秒自动关闭超时（手动干预提示出现后 20s 无操作则自动关闭 CLI）
4. **第 9 节简化**：工具卡片展示明确为不额外展示，CLI 输出直接在终端中显示

## Bridge Agent 前端组件设计文档第二次更新

1. **第 8.4 节 CLI Agent 配置增强**：工作空间默认自动填充主模型工作空间，头像根据 CLI 类型自动填充，所有字段可手动覆盖
2. **第 8.5 节 CLI 类型预设**：增加默认头像列（Claude/OpenCode/CodeBuddy 各有官方图标，custom 用 🔧）
3. **新增第 9 节：群聊项目配置**：新建群聊时可额外设置项目目录和项目说明文档路径；项目目录不覆盖 Agent 工作空间；支持多个说明文档注入 Agent 上下文；新增 GroupConfig.project 类型定义
4. **工具卡片展示调整为第 10 节**

## Bridge Agent 前端组件设计文档第三次更新

1. **第 9.2 节安全改进**：项目目录和项目说明文档路径改为**手动填写**，不提供文件/目录选择器（避免暴露服务器文件系统结构）
2. **第 9.3 节 CLI 启动目录调整**：设置群聊项目目录后，CLI Agent 在**项目目录中启动**（cwd = 群聊项目目录），未设置时回退到 Agent 工作空间
3. **第 9.5 节更新**：同步更新项目目录使用场景，明确 CLI 直接在项目目录启动，无需额外切换目录
4. **第 8.6 节 BridgeConfig 映射**：增加 effectiveCwd 注释，说明实际启动 cwd 取决于群聊项目配置

## Bridge Agent 文档体系第四次全面更新（12项修复）

根据用户反馈逐项更新了全部 6 个文档：

### architecture.md

1. **Skill 标注更新**：`❓ Bridge 不使用 Skill` → `✅ Bridge 有专属 Skill`
2. **BridgeConfig 类型扩展**：增加 `codebuddy` 类型和 `avatar` 字段
3. **新增 §3.3 进程隔离策略**：明确不同群聊中的 CLI Agent 进程完全独立，按 `(groupId, agentId)` 隔离

### frontend-components.md

4. **§3.3-3.4 纯文本提取机制**：CLI 输出完整渲染到前端终端组件，纯文本从 xterm.js buffer API 提取（非后端 ANSI 解析）
5. **§5.4 辅助 Agent 触发流程**：先将 TUI 渲染到页面，等渲染完成后再触发辅助 Agent，确保辅助 Agent 看到完整 TUI 状态
6. **§8.4.1 CLI 路径校验与测试启动**：Agent 管理页面增加启动命令校验和 `[🧪 测试启动]` 按钮
7. **§9.3.1 项目配置修改规则**：群聊创建后项目目录不可修改（锁定），项目说明文档可重新配置

### tui-rendering.md

8. **新增 §6 WebSocket 断线恢复策略**：后端环形缓冲区 + 重连重放 + 前端断线 UI 提示

### implementation.md

9. **删除 Phase 2 "提示符模式匹配"**（已废弃）
10. **更新文件清单**：新增 `bridge-assistant.ts`、`cli-agent-config.ts`，总行数 880→1430
11. **更新 Phase 4-5**：增加 CLI 路径校验、测试启动、群聊项目配置 UI、断线恢复等任务

### risks.md

12. **补充安全风险**：项目目录路径越权、目录选择器暴露服务器信息、不存在的项目路径

### README.md

- 版本号 v2.0→v3.0，更新导航表描述和改动范围数据

## 创建 bridge-assistant Skill 文件

创建了 `skills/bridge-assistant/SKILL.md`，辅助 Agent 的完整 Skill 定义：

- **§1 角色定位**：明确辅助 Agent 是 CLI 的监护者（不是业务讨论参与者、不是 CLI 替代品、不是决策者）
- **§2 触发时机**：描述被触发时收到的上下文数据结构（cliType、tuiContent、idleDuration 等）
- **§3 TUI 内容分析**：6 级优先级判断（等待确认 → 权限授权 → 错误 → 循环 → 长时间执行 → 无法判断）+ 不同 CLI 类型特征识别
- **§4 操作执行**：可自主执行的操作（确认/授权/回车/中断）+ 必须上报场景 + 安全约束（单次最多 1 次操作、不发危险指令）
- **§5 操作报告格式**：三类标准模板
- **§6 协作边界**：与 CLI Agent、Owner、其他 Agent 的关系
- **§7 异常处理**：自身失败、CLI 已退出、连续触发

同步更新了 `frontend-components.md` §7.3，将 Skill 内容改为引用实际文件 + 摘要

## 4 项补充策略写入设计文档（代码未改）

将用户提出的 4 个补充事项写入设计文档和 Skill 文件（回滚了之前误改的代码）：

### 补充 1：手动指定辅助 Agent + 管理员

- **文件**：`docs/design/group-chat-bridge/frontend-components.md` §9.2 + 新增 §9.2.1
- 新建群聊 UI 增加成员角色选择器（管理员 / 普通成员 / 辅助 Agent）
- 角色从"系统自动识别"改为"完全手动指定"
- 选择辅助 Agent 时自动添加 `__bridge-assistant__` 前缀

### 补充 2：@全体成员排除辅助 Agent

- **文件**：`docs/design/group-chat-bridge/frontend-components.md` 新增 §5.2.1
- 辅助 Agent 仅由系统触发，不参与 @all、broadcast、显式 @mention 等常规消息分发

### 补充 3：辅助 Agent agentId 标识设计

- **文件**：`docs/design/group-chat-bridge/architecture.md` 新增 §2.1.2
- **文件**：`docs/design/group-chat-bridge/implementation.md` 更新改动文件表
- agentId 使用 `__bridge-assistant__` 前缀标识
- 新增 `BRIDGE_ASSISTANT_PREFIX` 常量和 `isBridgeAssistant()` 辅助函数设计
- 消息分发排除规则（@all、broadcast、显式 mention 三处过滤）

### 补充 4：group-chat-reply Skill 不要 @辅助 Agent

- **文件**：`skills/group-chat-reply/SKILL.md`
- 新增"🛡️ 辅助 Agent（Bridge-Assistant）— 不要 @mention"章节
- 明确规则：永远不要 @mention 辅助 Agent，附带识别方式和示例

## Bridge Agent 设计文档缺陷修复和功能补充

根据设计评审发现的 18 个问题，按优先级修复了高优先级和中优先级问题：

### 高优先级修复

**#3 项目目录路径校验规则** (`frontend-components.md` 新增 §9.2.2)

- 多层校验：禁止路径模式、禁止路径列表、允许前缀限制、符号链接检测
- 前端校验提示（红色/黄色/绿色）
- 项目说明文档路径校验

**#6 群聊解散/成员移除时的 CLI 进程清理** (`architecture.md` 新增 §3.4)

- 群聊解散：SIGTERM → 5秒等待 → SIGKILL → 释放缓冲区
- 成员移除：同样清理流程
- 清理时机汇总表（解散、删除、移除、配置删除、系统关闭）

**#1 辅助 Agent 判断 CLI 已退出** (`frontend-components.md` 新增 §5.8)

- 状态判断表：卡住/正常运行/正常退出/异常退出/崩溃
- 完整判断流程图
- 退出状态的处理细节和报告模板
- CLI 重启的上下文恢复机制

### 中优先级修复

**#4 辅助 Agent 操作审计日志** (`frontend-components.md` 新增 §5.9)

- 存储位置：`~/.openclaw/audit/bridge-assistant/`
- 审计日志格式（JSON Lines，按月分文件）
- 保留期限：30 天

**#12 辅助 Agent 连续触发冷却机制** (`frontend-components.md` 新增 §5.10)

- 冷却时间：60 秒
- 单次运行最大触发次数：3 次
- 触发次数重置条件：CLI 重启/回复完成
- 达到上限时的处理

**#13 历史消息截断策略** (`implementation.md` 新增 §2.2.1)

- 最大消息数：30 条
- 最大字符数：50,000
- 截断优先级：最近消息优先、@mention 消息优先

**新功能：群聊上下文数量设置** (`implementation.md` §2.2.1)

- `GroupConfig.contextConfig` 类型扩展
- 前端配置 UI：最大消息数、最大字符数、是否包含系统消息
- 配置校验规则

### Skill 文件补充

**bridge-assistant/SKILL.md** 新增 §3.3 敏感信息识别与脱敏

- 敏感信息类型表（API Key、密码、Token、私钥等）
- 脱敏处理规则
- 报告中的脱敏示例

**bridge-cli/SKILL.md** 创建新文件

- §1 角色定位：CLI Agent 的身份和能力边界
- §2 群聊上下文理解：区分系统上下文和用户请求
- §3 协作规范：接收/执行/完成任务
- §4 敏感信息处理：脱敏规则、文件内容处理、错误信息处理
- §5 输出格式规范：进度报告、错误报告

### risks.md 更新

- 新增 §2.1 路径校验规则
- 新增 §2.2 敏感信息处理（防护层、模式匹配）
- 新增 §2.3 辅助 Agent 安全控制（触发冷却、次数限制、审计日志）

### implementation.md 改动文件表更新

- 新增文件：`audit-logger.ts`、`group-settings.ts`
- 扩展文件：`types.ts`（+15行 contextConfig）、`context-builder.ts`（+10行 截断逻辑）
- 合计行数：235 → 420 行

## Bridge Agent 代码实现

根据 `docs/design/group-chat-bridge/` 设计文档编写了完整的 Bridge Agent 代码实现。

### 新建文件（7 个，共 2551 行）

| 文件                                      | 行数 | 功能                                                                      |
| ----------------------------------------- | ---- | ------------------------------------------------------------------------- |
| `src/group-chat/bridge-types.ts`          | 169  | Bridge 配置、PTY 状态、辅助 Agent 触发状态等核心类型                      |
| `src/group-chat/bridge-pty.ts`            | 610  | PTY 进程管理器：创建/通信/尺寸同步/健康检查/空闲回收/异常重启/终止清理    |
| `src/group-chat/bridge-trigger.ts`        | 522  | Bridge 触发逻辑：双通道输出、上下文构建（# 注释语法）、截断策略、完成检测 |
| `src/group-chat/bridge-assistant.ts`      | 523  | 辅助 Agent 逻辑：触发冷却机制、TUI 分析、自主操作 CLI、操作审计记录       |
| `src/group-chat/terminal-events.ts`       | 54   | WebSocket 事件广播函数（group.terminal、group.terminalStatus）            |
| `src/group-chat/audit-logger.ts`          | 98   | 辅助 Agent 操作审计日志（JSONL 格式持久化）                               |
| `ui/src/ui/components/bridge-terminal.ts` | 575  | 前端终端组件：xterm.js 实时 TUI 渲染、可折叠/展开、纯文本提取、断线恢复   |

### 修改文件（9 个，+497 / -44 行）

| 文件                                  | 变更 | 改动内容                                                                                                                                                  |
| ------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/group-chat/types.ts`             | +29  | 扩展 GroupMemberRole（+bridge-assistant）、BRIDGE_ASSISTANT_PREFIX 常量、isBridgeAssistant()、GroupMember.bridge、GroupSessionEntry.project/contextConfig |
| `src/group-chat/agent-trigger.ts`     | +10  | 在 triggerAgentReasoning 开头增加 Bridge Agent 分叉判断（提前 return）                                                                                    |
| `src/group-chat/message-dispatch.ts`  | +28  | @all 展开排除辅助 Agent、显式 @mention 过滤辅助 Agent、broadcast 排除辅助 Agent                                                                           |
| `src/group-chat/context-builder.ts`   | +40  | Bridge Agent 角色标识、成员列表过滤辅助 Agent、Bridge Agent 专属约束条件                                                                                  |
| `src/group-chat/group-store.ts`       | +15  | createGroup 支持 bridge-assistant 角色、bridge 配置、project、contextConfig                                                                               |
| `src/group-chat/index.ts`             | +8   | 导出所有 Bridge Agent 模块                                                                                                                                |
| `src/gateway/server-methods/group.ts` | +149 | 新增 terminalResize/setContextConfig/terminalTextExtracted handler；群聊解散和成员移除时清理 PTY 进程                                                     |
| `ui/src/ui/controllers/group-chat.ts` | +164 | 扩展 GroupMember/GroupSessionMeta/GroupChatState 类型；新增 handleGroupTerminalEvent/handleGroupTerminalStatusEvent 事件处理器；Bridge 辅助方法           |
| `ui/src/ui/views/group-chat.ts`       | +98  | 集成 bridge-terminal 组件；成员列表过滤辅助 Agent；Bridge Agent 标签；renderActiveBridgeTerminals 函数                                                    |

### 验证结果

- **零 lint 错误**
- **TypeScript 错误数量**：修改前 150 → 修改后 146（减少 4 个，未引入任何新错误）
- 所有 `getLogger` 参数错误和 `number → string` 类型错误均为项目原有问题

### 前端依赖说明

前端终端组件 `bridge-terminal.ts` 需要安装 `@xterm/xterm` 和 `@xterm/addon-fit` 包：

```
pnpm add @xterm/xterm @xterm/addon-fit
```

组件已实现 graceful degradation：若 xterm.js 未安装，自动降级为纯文本模式。

## Bridge Agent 审查修复（3 项阻断性问题）

对照设计文档全面审查后，修复了 3 个问题：

### 修复 1：安装 @xterm/xterm 依赖（🔴 严重 → ✅ 已修复）

- `ui/` 目录下执行 `pnpm add @xterm/xterm @xterm/addon-fit`
- 此前 UI 整体无法构建

### 修复 2：app-gateway.ts 事件接线（🔴 严重 → ✅ 已修复）

- `ui/src/ui/app-gateway.ts` 中 `handleGatewayEvent()` 缺少 `group.terminal` 和 `group.terminalStatus` 事件的处理分支
- 添加了两个事件分支 + 对应的 import（`handleGroupTerminalEvent`、`handleGroupTerminalStatusEvent`、`GroupTerminalPayload`、`GroupTerminalStatusPayload`）
- 此前终端数据永远到不了前端

### 修复 3：前后端状态枚举映射（🟡 偏差 → ✅ 已修复）

- 后端 `BridgePtyStatus`："idle" | "running" | "stuck" | "error" | "offline"
- 前端 `BridgeTerminalStatus`："idle" | "working" | "completed" | "error" | "disconnected"
- `GroupTerminalStatusPayload.status` 改为 `string` 类型接受所有后端值
- 新增 `mapPtyStatusToTerminalStatus()` 映射函数（running→working, stuck→error, offline→disconnected）
- `handleGroupTerminalStatusEvent()` 使用映射后的值

### 验证结果

- UI 构建成功（`pnpm build` 通过）
- 零 lint 错误
- xterm.js 被正确打包为独立 chunk（329KB gzip 83KB）

## @mariozechner/pi-ai 0.57.1 升级兼容性修复（19 个 TS 错误）

`@mariozechner/pi-ai` 升级到 0.57.1 后有两类 breaking change：

### 1. onPayload 签名变更（16 处）

- 旧签名：`onPayload?: (payload: unknown) => ...`
- 新签名：`onPayload?: (payload: unknown, model: Model<Api>) => ...`
- 修复方式：所有 wrapper 的 `onPayload` 回调添加 `model` 参数（`(payload, m) => ...`），并传递给链式调用的原始 `onPayload`
- 涉及文件（8 个）：
  - `src/agents/anthropic-payload-log.ts`（1 处）
  - `src/agents/openai-ws-stream.ts`（1 处）
  - `src/agents/pi-embedded-runner/anthropic-stream-wrappers.ts`（1 处）
  - `src/agents/pi-embedded-runner/extra-params.ts`（3 处）
  - `src/agents/pi-embedded-runner/moonshot-stream-wrappers.ts`（2 处）
  - `src/agents/pi-embedded-runner/openai-stream-wrappers.ts`（2 处）
  - `src/agents/pi-embedded-runner/proxy-stream-wrappers.ts`（3 处）
  - `src/agents/pi-embedded-runner/run/attempt.ts`（2 处）

### 2. OAuth 函数导出路径变更（3 处）

- `getOAuthApiKey`/`getOAuthProviders`：从 `@mariozechner/pi-ai` → `@mariozechner/pi-ai/oauth`
  - 文件：`src/agents/auth-profiles/oauth.ts`
- `loginOpenAICodex`：从 `@mariozechner/pi-ai` → `@mariozechner/pi-ai/oauth`
  - 文件：`src/commands/openai-codex-oauth.ts`

### 验证结果

- `tsc -p tsconfig.plugin-sdk.dts.json` 通过（零错误）

## Bridge Agent Phase 4 UI 完善（4 项功能全部完成）

### 功能 1：Agent 详情页展示 bridge 配置

- **文件**：`ui/src/ui/views/agents.ts`、`ui/src/ui/app-render.ts`
- `AgentsProps` 新增 `agentFileContents` 字段
- `renderAgentOverview` 新增 `agentFileContents` 参数
- 新增 `parseBridgeConfig()` 函数：从 agent 文件列表解析 bridge.json
- 新增 `renderBridgeConfigSection()` 函数：在 Overview 面板中展示 CLI 类型、命令、参数、工作目录、超时、环境变量等
- CLI Agent 检测：通过 identity theme 是否以 "CLI Agent" 开头判断

### 功能 2：群聊创建时支持 bridge-assistant 角色

- **文件**：`ui/src/ui/controllers/group-chat.ts`、`ui/src/ui/views/group-chat.ts`、`ui/src/ui/app-render.ts`
- `GroupCreateDialogState.selectedAgents` 的 role 类型扩展为 `"assistant" | "member" | "bridge-assistant"`
- 创建对话框中每个成员选中后显示角色下拉选择器（assistant / member / bridge-assistant）
- 新增 `projectDirectory` 和 `projectDocs` 字段到 `GroupCreateDialogState`
- 创建对话框底部新增项目目录和项目文档配置区域（可选）
- `createGroup()` 函数支持传递 `project` 参数

### 功能 3：群聊设置中的项目配置 UI

- **文件**：`ui/src/ui/views/group-chat.ts`、`ui/src/ui/controllers/group-chat.ts`、`ui/src/ui/app-render.ts`、`src/gateway/server-methods/group.ts`
- 后端新增 `handleGroupSetProjectDocs` RPC handler（`group.setProjectDocs`）
- 前端新增 `updateGroupProjectDocs()` 控制器函数
- 群聊设置面板新增"Project Configuration"区域：
  - 项目目录：只读展示（🔒 锁定，创建后不可修改）
  - 项目文档：可动态添加/删除文档路径列表
- `shouldRefreshMeta` 集合中添加 `project_docs_changed` 事件

### 功能 4：群聊设置中的上下文配置 UI

- **文件**：`ui/src/ui/views/group-chat.ts`、`ui/src/ui/controllers/group-chat.ts`、`ui/src/ui/app-render.ts`
- 前端新增 `updateGroupContextConfig()` 控制器函数
- 群聊设置面板新增"Context Configuration"区域：
  - Max Messages（5-100，默认 30）
  - Max Characters（10,000-200,000，默认 50,000）
  - Include System Messages（checkbox）
- 复用已有的 `group.setContextConfig` RPC（后端已完整实现）

### 验证结果

- UI 构建成功（`pnpm build` 通过）
- 零 lint 错误
- 零 TypeScript 错误
